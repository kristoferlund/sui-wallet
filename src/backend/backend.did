type PublicKey = vec nat8;
type PublicKeyResult = variant { Ok : PublicKey; Err : text };
type BytesResult = variant { Ok : vec nat8; Err : text };

service : () -> {
  get_public_key : () -> (PublicKeyResult);
  sign : (msg: vec nat8) -> (BytesResult);
}
